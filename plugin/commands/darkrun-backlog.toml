description = "Parking lot for ideas not yet ready to become a Run — add, list, review, or promote a backlog item"
prompt = """
Work the darkrun backlog. Follow the `darkrun-backlog` skill:

Call `darkrun_backlog` with an optional `action` ("list", "add", "review", "promote") and
`description`, then follow the returned instructions. Default action is "list".

When promoting an item, hand off to `/darkrun:darkrun-start` (or `/darkrun:darkrun-quick` for small
items) to scaffold the lifecycle.
"""
