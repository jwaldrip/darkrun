---
description: Show the darkrun changelog / release notes, optionally for a specific version
argument-hint: [version]
---

Show darkrun release notes.

Call `darkrun_changelog` with an optional `version` (`$ARGUMENTS`); with none, show the latest. Lead with the newest changes. See the `darkrun-changelog` skill.
