---
description: Show a darkrun Run's state — Stations, Units, completion criteria, and Checkpoint status
argument-hint: [run slug]
---

Show the darkrun Run state for `$ARGUMENTS` (omit to use the active Run).

Call `darkrun_run_show` and `darkrun_unit_list`, then lead with the current Station/phase and what's next. See the `darkrun-show` skill.
