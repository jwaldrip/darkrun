---
description: Submit feedback or a bug report about darkrun itself — synthesized into a structured, actionable report
---

Submit a darkrun feedback/bug report.

Gather what happened, expected, and repro; synthesize into a structured summary; confirm; then call `darkrun_report { message }`. This is feedback about darkrun the tool — to route rework inside a Run, use `darkrun_feedback_create`. See the `darkrun-report` skill.
